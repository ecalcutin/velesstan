{
  "name": "velesstan",
  "version": "1.0.0",
  "scripts": {
    "build": "turbo run build --",
    "dev": "dotenv -- npx turbo run dev --",
    "lint": "npx turbo run lint --",
    "test": "npx turbo run test --",
    "prettier": "npx turbo run prettier -- --write",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5",
    "ts-node": "^10.9.2",
    "turbo": "^1.13.0",
    "typescript": "^5.4.3"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "packageManager": "npm@10.5.0"
}
